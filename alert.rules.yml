groups:
- name: fastapi-alerts
  rules:
  - alert: HighRequestCount
    expr: rate(request_count_total[1m]) > 80
    for: 5m
    labels:
      severity: warning
    annotations:
      description: "请求量过高"
